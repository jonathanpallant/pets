name: format
on: [push, pull_request, merge_group]
jobs:
  format-lib:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          cargo fmt -- --check
  format-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          cd examples
          cargo fmt -- --check
